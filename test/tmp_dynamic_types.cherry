type A = string x when x.length() > 2 default "testy";
type B<T> = T | string;
type C = B<int>;

C c = 5;

A a;

Standard.println(a);

Standard.println("hello is A: {}, no is A: {}", "hello" is A, "no" is A);

Standard.println("dynamic-ok {}", c);
