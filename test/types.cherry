use internal;

type int = __internal_int;
type string = __internal_string;
type any = __internal_any;

class Array<T = any> {

    int length = 0;
    __internal_list underlying_list = [];

    get [int x] {
        if (x >= length) {
            underlying_list.length = length * 2 + 1;
            
        }
        return 
    }

    set [int x] {

    }

    append(T value) {

    }

}