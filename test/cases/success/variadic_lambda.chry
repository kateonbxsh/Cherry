function sum = (int ...xs) => {
  int i = 0;
  int total = 0;
  while (i < xs.size()) {
    total = total + xs.get(i);
    i = i + 1;
  }
  return total;
};

int a = sum();
int b = sum(4);
int c = sum(1, 2, 3, 4);
Standard.println("OK variadic_lambda {} {} {}", a, b, c);
